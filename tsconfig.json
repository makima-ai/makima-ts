{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016" /* Set the JavaScript language version for emitted JavaScript. */,
    "module": "commonjs" /* Specify the module system (e.g., CommonJS for Node.js compatibility). */,
    "esModuleInterop": true /* Emit additional code to ease support for importing CommonJS modules. */,
    "forceConsistentCasingInFileNames": true /* Ensure imports have consistent casing. */,
    "strict": true /* Enable all strict type-checking options. */,
    /* Emit */
    "declaration": true /* Generate .d.ts files. */,
    "declarationDir": "./dist" /* Specify the output directory for declaration files. */,
    "emitDeclarationOnly": true /* Emit only .d.ts files without JavaScript output. */,
    "outDir": "./dist" /* Specify the output folder for all emitted files. */,
    /* Module Resolution */
    "moduleResolution": "node" /* Use Node.js-style resolution. */,
    "resolveJsonModule": true /* Enable importing JSON files. */,
    /* Type Checking */
    "skipLibCheck": true /* Skip type checking of declaration files. */,
    /* Completeness */
    "isolatedModules": true /* Ensure each file can be safely transpiled independently. */
  },
  "include": ["src/**/*" /* Include all files in the src folder. */],
  "exclude": [
    "node_modules" /* Exclude the node_modules folder. */,
    "dist" /* Exclude the dist folder to avoid accidental inclusion. */
  ]
}
